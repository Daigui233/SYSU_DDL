# Optional dependencies for turnsign_ocr_api.py.
# Install these only on the machine that runs TurnSign OCR + AI Studio interpretation.
erniebot>=0.5.0
paddlepaddle==3.0.0; sys_platform == "win32"
paddleocr>=2.10.0,<3.0.0
numpy<2.0
pillow<11
albumentations<2.0.0
opencv-python-headless==4.9.0.80

# PaddleOCR also needs PaddlePaddle. Choose the wheel that matches the board/Python/CPU:
#   pip install paddlepaddle
# On RK3588S ARM Linux this may require a platform-specific wheel or a prebuilt image.
